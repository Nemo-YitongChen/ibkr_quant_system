# Change Archive: Watchlist Expansion Recommendations

Date: 2026-05-29

## Context

Small-account paper auto-submit is still constrained by whole-share tradability, expected cost, fee/spread assumptions, and strict submit gates. The previous dashboard showed watchlist expansion counts and top reject reasons, but it did not provide a market-level next action.

## Changes

- Added common watchlist expansion summary helpers in `src/common/watchlist_expansion.py`.
- Consolidated rejected candidate reason aggregation so CLI artifacts and dashboard blocks use the same logic.
- Added market-level follow-up recommendations for zero-selected markets.
- Added primary recommendation fields to the watchlist expansion dashboard payload and v2 block.
- Added the same summary fields to `watchlist_expansion_summary.json` generated by `expand_investment_watchlists`.
- Kept existing risk, edge, cost, market-rule, and submit gates unchanged.

## Current Local Diagnosis

Latest local dashboard generation reported:

- candidate rows: `65`
- selected rows: `2`
- zero-selected markets: `3`
- primary recommendation market: `ASX`
- primary recommendation reason: `expected_cost_above_max`
- primary recommendation action: `calibrate_cost_or_expand_lower_cost_etfs`

Top rejected candidate reasons:

- `expected_cost_above_max`: `46`
- `whole_share_not_tradable`: `46`
- `whole_share_edge_margin_below_min`: `27`
- `last_close_above_account_cap`: `19`
- `score_below_min`: `18`

Market recommendations:

- `ASX`: calibrate cost assumptions or expand lower-cost ETF candidates.
- `HK`: calibrate cost assumptions or expand lower-cost ETF candidates.
- `XETRA`: calibrate cost assumptions or expand lower-cost ETF candidates.

## Validation

- `python -m py_compile src/common/watchlist_expansion.py src/tools/generate_dashboard.py src/tools/dashboard_blocks.py src/tools/expand_investment_watchlists.py`
- `pytest -q -p no:cacheprovider tests/test_watchlist_expansion.py tests/test_generate_dashboard_helpers.py tests/test_dashboard_blocks.py tests/test_investment_workflow_smoke.py::test_investment_workflow_cli_smoke_generates_contract_artifacts`
- `pytest -q -p no:cacheprovider tests/test_supervisor_cli.py::SupervisorCliTests::test_dashboard_market_data_health_overview_marks_nonresearch_fallback_market_as_attention`
- `python -m src.tools.generate_dashboard --config config/supervisor.yaml --out_dir runtime_data/paper_investment_only_duq152001/reports_supervisor`

Full local integration tier was started but terminated after several minutes without a completed result. The previously failing integration assertion was verified directly.

## Next Technical Path

1. Refresh per-market candidate reports for ASX, HK, and XETRA after the next data window.
2. Compare expected cost assumptions against actual paper fill/slippage evidence before lowering any cost thresholds.
3. Expand ETF-first universes with lower-price, high-liquidity, whole-share tradable symbols.
4. Keep automatic submit limited to one high-quality small paper order until fill, slippage, and no-order diagnostics improve.
